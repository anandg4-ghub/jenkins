apiVersion: v1
kind: Service
metadata:
  name: hello-app-service
  labels:
    app: hello-app
spec:
  type: NodePort  # Change to LoadBalancer or ClusterIP as needed
  selector:
    app: hello-app
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
